1.error:QMainWindow: No such file or directory
解决办法：检查.pro文件中是否添加QT += core gui printsupport
greaterThan(QT_MAJOR_VERSION, 4): QT += widgets
并检查.pro中是否有多余的配置
2.error: GL/gl.h: No such file or directory
解决办法：sudo apt-get install mesa-common-dev
再次执行，又报错：cannot find -lGL
sudo apt-get install libgl1-mesa-dev
至此，成功
3.
